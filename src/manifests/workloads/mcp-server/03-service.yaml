apiVersion: v1
kind: Service
metadata:
  name: mcp-server-svc
  namespace: inference
  labels:
    app.kubernetes.io/name: mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-server
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP